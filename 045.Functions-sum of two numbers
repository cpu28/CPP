#include <iostream>

using namespace std;

float add(float x, float y)
{
    int z;
    z=x+y;
    return z;
}

int main()
{
    int a, b,c;
    cout<<"Enter two numbers:";
    cin>>a>>b;
    c=add(a,b);
    cout<<"Sum of "<<a<<" and "<<b<<" is "<<c;
    return 0;
}
