#include <iostream>

using namespace std;

int main()
{
    int n,sum;
    cout<<"Enter the value of 'n' upto which the sum of the first 'n' natural numbers is to be computed:";
    cin>>n;
    sum=n*(n+1)/2;
    cout<<"sum of first "<<n<<" natural numbers is: "<<sum;
    return 0;
}
