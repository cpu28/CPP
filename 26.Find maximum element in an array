#include<iostream>
using namespace std;
int main()
{
    int A[7]={10,8,5,2,4,1,9};
    int max=A[0];
    for (int x:A)
    {
        if (x>=max)
        {
            max=x;
        }
    }
    cout<<"maximum is "<<max;
    return 0;
}
